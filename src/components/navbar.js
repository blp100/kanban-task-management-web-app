"use client";
import { Box } from "@chakra-ui/react";
import Logo from "./logo";

const Navbar = () => {
  return (
    <Box>
      <Logo display={"inline-block"} />
    </Box>
  );
};

export default Navbar;
